<Window x:Class="DataGUITests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:domain="clr-namespace:SyncSaberLib.Data;assembly=SyncSaberLib"
        xmlns:local="clr-namespace:DataGUITests"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="SongViewSource" d:DesignSource="{d:DesignInstance {x:Type domain:Song}, CreateList=True}"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource SongViewSource}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="DataGridCell" x:Name="SongNameCell" />
        </Grid.Resources>
        <DataGrid Grid.Row="1" x:Name="SongGrid" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="10,10,10,10" 
                  ItemsSource="{ Binding }" AutoGenerateColumns="True" EnableRowVirtualization="True">
        </DataGrid>
                  

    </Grid>
</Window>
